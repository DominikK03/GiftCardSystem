<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}{{ 'activation.page_title'|trans({}, 'ui') }}{% endblock %}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,600;9..144,700&family=Public+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen flex flex-col bg-[#f7f6f3] text-slate-800 [font-family:'Public_Sans',sans-serif]">
    <div class="fixed inset-0 -z-10 bg-[radial-gradient(900px_450px_at_10%_-5%,rgba(16,115,104,0.16),transparent_70%),radial-gradient(700px_360px_at_100%_0%,rgba(209,122,54,0.16),transparent_74%)]"></div>

    <header class="sticky top-0 z-20 border-b border-[#e6e1d8] bg-[#f7f6f3]/90 backdrop-blur-md">
        <div class="max-w-5xl mx-auto px-4 py-4 flex items-center justify-between gap-4">
            <a href="{{ path('public_home') }}" class="text-xl [font-family:'Fraunces',serif] font-semibold tracking-tight text-slate-900">
                Gift<span class="text-emerald-700">Card</span> System
            </a>
            <nav class="flex items-center gap-2 text-sm font-semibold">
                <a href="{{ path('activation_form') }}" class="px-4 py-2 rounded-full border border-emerald-700/25 bg-white text-emerald-800 hover:-translate-y-0.5 transition">Aktywuj Karte</a>
                <a href="{{ path('my_cards_form') }}" class="px-4 py-2 rounded-full border border-[#d6d0c6] bg-white text-slate-700 hover:-translate-y-0.5 transition">Moje Karty</a>
            </nav>
        </div>
    </header>

    <main class="flex-1 flex items-start justify-center py-10">
        <div class="w-full max-w-xl mx-auto px-4">
            {% for type, messages in app.flashes %}
                {% for message in messages %}
                    <div class="mb-4 p-4 rounded-xl border text-sm font-medium shadow-sm {% if type == 'error' %}bg-red-50 text-red-900 border-red-200{% elseif type == 'success' %}bg-emerald-50 text-emerald-900 border-emerald-200{% else %}bg-amber-50 text-amber-900 border-amber-200{% endif %}">
                        {{ message|trans({}, 'ui') }}
                    </div>
                {% endfor %}
            {% endfor %}

            {% block content %}{% endblock %}
        </div>
    </main>

    <footer class="border-t border-[#e6e1d8] bg-white/70 py-6 mt-auto">
        <div class="max-w-5xl mx-auto px-4 text-xs text-slate-500 flex flex-col sm:flex-row sm:items-center sm:justify-between gap-2">
            <span>&copy; {{ 'now'|date('Y') }} GiftCard System Sp. z o.o.</span>
            <span>ul. Przemyslowa 12, Warszawa · +48 22 555 44 33 · kontakt@giftcardsystem.pl</span>
        </div>
    </footer>
</body>
</html>
